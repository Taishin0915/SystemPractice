# 開発環境用のDockerfile
FROM node:18-alpine

WORKDIR /app

# 必要なパッケージをインストール（PrismaのためにOpenSSLが必要）
RUN apk add --no-cache openssl

# パッケージファイルをコピーして依存関係をインストール
COPY package.json package-lock.json* ./
RUN npm install

# アプリケーションファイルをコピー
COPY . .

# Prismaクライアントを生成
RUN npx prisma generate

# ポートを公開
EXPOSE 3000

# 開発サーバーを起動
CMD ["npm", "run", "dev"]




